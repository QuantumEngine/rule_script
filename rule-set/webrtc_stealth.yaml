# NAME: WebRTC Stealth by Process Names
# UPDATED: 2026-03-16 19:14

payload:
  # Chrome
  - AND,((NETWORK,UDP),(DST-PORT,3478),(PROCESS-NAME,chrome.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19302),(PROCESS-NAME,chrome.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19305),(PROCESS-NAME,chrome.exe))
  - AND,((NETWORK,UDP),(DST-PORT,5349),(PROCESS-NAME,chrome.exe))
  # Comet
  - AND,((NETWORK,UDP),(DST-PORT,3478),(PROCESS-NAME,comet.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19302),(PROCESS-NAME,comet.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19305),(PROCESS-NAME,comet.exe))
  - AND,((NETWORK,UDP),(DST-PORT,5349),(PROCESS-NAME,comet.exe))
  # Antigravity
  - AND,((NETWORK,UDP),(DST-PORT,3478),(PROCESS-NAME,Antigravity.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19302),(PROCESS-NAME,Antigravity.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19305),(PROCESS-NAME,Antigravity.exe))
  - AND,((NETWORK,UDP),(DST-PORT,5349),(PROCESS-NAME,Antigravity.exe))
  # VSCode
  - AND,((NETWORK,UDP),(DST-PORT,3478),(PROCESS-NAME,Code.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19302),(PROCESS-NAME,Code.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19305),(PROCESS-NAME,Code.exe))
  - AND,((NETWORK,UDP),(DST-PORT,5349),(PROCESS-NAME,Code.exe))
  # Cursor
  - AND,((NETWORK,UDP),(DST-PORT,3478),(PROCESS-NAME,Cursor.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19302),(PROCESS-NAME,Cursor.exe))
  - AND,((NETWORK,UDP),(DST-PORT,19305),(PROCESS-NAME,Cursor.exe))
  - AND,((NETWORK,UDP),(DST-PORT,5349),(PROCESS-NAME,Cursor.exe))